import { MostRecentNamePipe } from './most-recent-name.pipe';

describe('MostRecentNamePipe', () => {
  it('create an instance', () => {
    const pipe = new MostRecentNamePipe();
    expect(pipe).toBeTruthy();
  });
});
